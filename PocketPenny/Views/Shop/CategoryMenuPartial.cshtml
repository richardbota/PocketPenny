@model IEnumerable<PocketPenny.Models.ViewModels.Shop.CategoryVM>

<h3>Categories</h3>

<ul class="categories">
    @foreach (var item in Model)
    {
        <li class="btn btn-default" role="button"><a href="/shop/category/@item.Slug.ToLower()">@item.Name</a></li>
    }
</ul>

